<!DOCTYPE html>
<html lang="en">
  <head>
    <meta http-equiv="X-UA-Compatible" content="IE=edge" />
    <title>Kaiadmin - Bootstrap 5 Admin Dashboard</title>
    <meta
      content="width=device-width, initial-scale=1.0, shrink-to-fit=no"
      name="viewport"
    />
    <link
      rel="icon"
      href="/assets2/img/kaiadmin/  .ico"
      type="image/x-icon"
    />

    <!-- Fonts and icons -->
    <script src="/assets2/js/plugin/webfont/webfont.min.js"></script>
    <script>
      WebFont.load({
        google: { families: ["Public Sans:300,400,500,600,700"] },
        custom: {
          families: [
            "Font Awesome 5 Solid",
            "Font Awesome 5 Regular",
            "Font Awesome 5 Brands",
            "simple-line-icons",
          ],
          urls: ["/assets2/css/fonts.min.css"],
        },
        active: function () {
          sessionStorage.fonts = true;
        },
      });
    </script>

    <!-- CSS Files -->
    <link rel="stylesheet" href="/assets2/css/bootstrap.min.css" />
    <link rel="stylesheet" href="/assets2/css/plugins.min.css" />
    <link rel="stylesheet" href="/assets2/css/kaiadmin.min.css" />
    <link rel="stylesheet" href="/assets2/css/loginStyle.css" />
    <!-- Sweet Alert CSS -->
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/sweetalert2@11/dist/sweetalert2.min.css">
    <!-- cropping JS -->
    <link href="https://unpkg.com/cropperjs/dist/cropper.min.css" rel="stylesheet">
    <!-- adminStyle -->
     <link rel="stylesheet" href="/assets2/css/adminValidationStyle.css">

     <style>
      .modal { 
        display: none; 
        position: fixed; 
        top: 0; 
        left: 0; 
        width: 700px; 
        height: 700px; 
        background-color: rgba(0,0,0,0.7); 
        justify-content: center; 
        align-items: center; 
      }
      .modal-content { 
        background-color: white; 
        padding: 20px; 
        border-radius: 8px; 
        max-width: 90%; 
        max-height: 90%; 
        overflow: auto; 
      }
      .cropper-container {
        max-width: 100%;
        max-height: 80vh;
      }
      .button-container {
        display: flex;
        justify-content: space-between;
        margin-top: 10px;
      }
    </style>
  </head>
  <body>
    

    <div class="wrapper">
      <!-- Sidebar -->
      <div class="sidebar" data-background-color="dark">
        <div class="sidebar-logo">
          <!-- Logo Header -->
          <div class="logo-header" data-background-color="dark">
            <a href="#" class="logo">
              <h2 class="text-white fw-bold"><i class="fas fa-shopping-cart fs-4 text-secondary"></i> WingCart</h2>
            </a>
            <!-- <div class="nav-toggle">
              <button class="btn btn-toggle toggle-sidebar">
                <i class="gg-menu-right"></i>
              </button>
              <button class="btn btn-toggle sidenav-toggler">
                <i class="gg-menu-left"></i>
              </button>
            </div> -->
            <button class="topbar-toggler more">
              <i class="gg-more-vertical-alt"></i>
            </button>
          </div>
          <!-- End Logo Header -->
        </div>
        <div class="sidebar-wrapper scrollbar scrollbar-inner">
          <div class="sidebar-content">
            <ul class="nav nav-secondary">
              <li class="nav-item active">
                <a href="/admin/dashboard">
                  <i class="fas fa-home"></i>
                  <p>Dashboard</p>
                </a>
              </li>
              <li class="nav-item">
                <a href="/admin/userManagement">
                  <i class="fas fa-user-cog"></i>
                  <p>User Management</p>
                </a>
              </li>
              <li class="nav-item">
                <a href="/admin/Management">
                  <i class="fas fa-layer-group"></i>
                  <p>Category Management</p>

                </a>
              </li>
              <li class="nav-item">
                <a href="/admin/productManagement">
                    <i class="fas fa-box-open"></i>
                  <p>Product Management</p>

                </a>
              </li>
              <li class="nav-item">
                <a href="#tables">
                    <i class="fas fa-shopping-cart"></i>
                  <p>Order Management</p>

                </a>
              </li>
              <li class="nav-item">
                <a href="#maps">
                  <i class="fas fa-sign"></i>
                  <p>Banner Management</p>

                </a>
              </li>
              <li class="nav-item">
                <a href="#charts">
                  <i class="fas fa-tags"></i>
                  <p>Offers & Coupons</p>

                </a>
              </li>
              <li class="nav-item">
                <a href="/admin/salesReport">
                  <i class="fas fa-chart-bar"></i>
                  <p>Sales Report</p>
                </a>
              </li>
              
            </ul>
          </div>
        </div>
      </div>
      <!-- End Sidebar -->

      <div class="main-panel">
        <div class="main-header">
          <div class="main-header-logo">
            <!-- Logo Header -->
            <div class="logo-header" data-background-color="dark">
              <a href="index.html" class="logo">
                <img
                  src="/assets2/img/kaiadmin/logo_light.svg"
                  alt="navbar brand"
                  class="navbar-brand"
                  height="20"
                />
              </a>
              <div class="nav-toggle">
                <button class="btn btn-toggle toggle-sidebar">
                  <i class="gg-menu-right"></i>
                </button>
                <button class="btn btn-toggle sidenav-toggler">
                  <i class="gg-menu-left"></i>
                </button>
              </div>
              <button class="topbar-toggler more">
                <i class="gg-more-vertical-alt"></i>
              </button>
            </div>
            <!-- End Logo Header -->
          </div>
          <!-- Navbar Header -->
          <nav
            class="navbar navbar-header navbar-header-transparent navbar-expand-lg border-bottom"
          >
            <div class="container-fluid">
              <nav
                class="navbar navbar-header-left navbar-expand-lg navbar-form nav-search p-0 d-none d-lg-flex"
              >

              <!-- Search -->
                <div class="input-group">
                  <div class="input-group-prepend">
                    <button type="submit" class="btn btn-search pe-1">
                      <i class="fa fa-search search-icon"></i>
                    </button>
                  </div>
                  <input
                    type="text"
                    placeholder="Search ..."
                    class="form-control"
                  />
                </div>
              </nav>

              <ul class="navbar-nav topbar-nav ms-md-auto align-items-center">
                <li
                  class="nav-item topbar-icon dropdown hidden-caret d-flex d-lg-none"
                >
                  <a
                    class="nav-link dropdown-toggle"
                    data-bs-toggle="dropdown"
                    href="#"
                    role="button"
                    aria-expanded="false"
                    aria-haspopup="true"
                  >
                    <i class="fa fa-search"></i>
                  </a>
                  <ul class="dropdown-menu dropdown-search animated fadeIn">
                    <form class="navbar-left navbar-form nav-search">
                      <div class="input-group">
                        <input
                          type="text"
                          placeholder="Search ..."
                          class="form-control"
                        />
                      </div>
                    </form>
                  </ul>
                </li>
                <li class="nav-item topbar-icon dropdown hidden-caret">
                  <ul
                    class="dropdown-menu messages-notif-box animated fadeIn"
                    aria-labelledby="messageDropdown"
                  >
                  </ul>
                </li>

                <li class="nav-item topbar-user dropdown hidden-caret">
                  <a
                    class="dropdown-toggle profile-pic"
                    data-bs-toggle="dropdown"
                    href="#"
                    aria-expanded="false"
                  >
                    <div class="avatar-sm">
                      <i class="fas fa-user-alt"></i>
                    </div>
                    <span class="profile-username">
                      <span class="fw-bold">Uvais</span>
                    </span>
                  </a>
                  <ul class="dropdown-menu dropdown-user animated fadeIn">
                    <div class="dropdown-user-scroll scrollbar-outer">
                      <li>
                        <div class="user-box align">
                          <div class="avatar-lg">
                            <i class="fas fa-user-alt"></i>
                          </div>
                          <div class="u-text">
                            <h4>Uvais</h4>
                            <p class="text-muted">hello@example.com</p>
                            <a
                              href="profile.html"
                              class="btn btn-xs btn-secondary btn-sm"
                              >View Profile</a
                            >
                          </div>
                        </div>
                      </li>
                      <li>
                        <div class="dropdown-divider"></div>
                        <a class="dropdown-item" href="#">My Profile</a>
                        <a class="dropdown-item" href="/admin/logout">Logout</a>
                      </li>
                    </div>
                  </ul>
                </li>
              </ul>
            </div>
          </nav>
          <!-- End Navbar -->
        </div>

        <div class="container">
          <div class="page-inner">
            <div class="page-header">
              <h4 class="page-title">Edit Product</h4>
              <ul class="breadcrumbs">
                <li class="nav-home">
                  <a href="/admin/dashboard">
                    <i class="icon-home"></i>
                  </a>
                </li>
                <li class="separator">
                  <i class="icon-arrow-right"></i>
                </li>
                <li class="nav-item">
                  <a href="/admin/productManagement">Edit Management</a>
                </li>
                <li class="separator">
                  <i class="icon-arrow-right"></i>
                </li>
                <li class="nav-item">
                  <p>Add Product</p>
                </li>
              </ul>
            </div>
<!-- //////////////////////////////////////// -->

<div class="row justify-content-center my-5">
    <div class="col-md-8">
      <form id="editProductForm" class="form-horizontal" enctype="multipart/form-data">
        <fieldset>

          <p class="text-danger main-error"></p>

          <div class="row">
            <!-- Left column -->
            <div class="col-md-6">
              <!-- Text input for Product Name -->
              <div class="form-group">
                <label class="control-label errorMsg" for="productName">Product Name</label>
                <input id="productName" name="productName" class="form-control" type="text" value="<%= locals.product ? product.name : '' %>">
              </div>
  
              <!-- Text input for Model -->
              <div class="form-group">
                <label class="control-label errorMsg" for="model">Model</label>
                <input id="model" name="model" class="form-control" type="text" value="<%= locals.product ? product.model : '' %>">
              </div>
  
              <!-- Textarea for Description -->
              <div class="form-group">
                <label class="control-label errorMsg" for="description">Description</label>
                <textarea id="description" name="description" class="form-control" rows="3"><%= locals.product ? product.description : '' %></textarea>
              </div>
  
              <!-- Dropdown for Type -->
              <div class="form-group">
                <label class="control-label errorMsg" for="type">Type</label>
                <select id="type" name="type" class="form-control">
                  <option value="" disabled>-Select-</option>
                  <option value="Digital" <%= locals.product && product.type === "Digital" ? "selected" : "" %>>Digital</option>
                  <option value="Analog" <%= locals.product && product.type === "Analog" ? "selected" : "" %>>Analog</option>
                  <option value="Digital & Analog" <%= locals.product && product.type === "Digital & Analog" ? "selected" : "" %>>Digital & Analog</option>
                </select>
              </div>
  
              <!-- Text input for Strap Type -->
              <div class="form-group">
                <label class="control-label errorMsg" for="strapType">Strap Type</label>
                <input id="strapType" name="strapType" class="form-control" type="text" value="<%= locals.product ? product.strapType : '' %>">
              </div>
            </div>
  
            <!-- Right column -->
            <div class="col-md-6">
              <!-- Text input for Color -->
              <div class="form-group">
                <label class="control-label errorMsg" for="color">Color</label>
                <input id="color" name="color" class="form-control" type="text" value="<%= locals.product ? product.color : '' %>">
              </div>
  
              <div class="form-group">
                <label class="control-label errorMsg" for="category">Category</label>
                <select id="category" name="category" class="form-control">
                  <option value="" disabled>-Select-</option>
                  <% if (locals.categories && categories.length) { %>
                    <% categories.forEach((category) => { %>
                      <option value="<%= category._id %>" <%= locals.product && product.category.toString() === category._id.toString() ? "selected" : "" %>>
                        <%= category.name %>
                      </option>
                    <% }) %>
                  <% } %>
                </select>
              </div>
  
              <!-- Dropdown for Brand -->
              <div class="form-group">
                <label class="control-label errorMsg" for="brand">Brand</label>
                <select id="brand" name="brand" class="form-control">
                  <option value="" selected disabled>-Select-</option>
                  <% if (locals.brands && brands.length) { %>
                    <% brands.forEach((brand) => { %>
                        <option value="<%= brand._id %>" <%= locals.product && product.brand.toString() === brand._id.toString() ? "selected" : "" %>>
                          <%= brand.name %>
                        </option>
                    <% }) %>
                  <% } %>
                </select>
              </div>
  
              <!-- Text input for Stock -->
              <div class="form-group">
                <label class="control-label errorMsg" for="stock">Stock</label>
                <input id="stock" name="stock" class="form-control" type="number" value="<%= locals.product ? product.stock : '' %>">
              </div>

          <!-- Text input for Price -->
          <div class="form-group">
            <label class="control-label errorMsg" for="price">Price</label>
            <input id="price" name="price" class="form-control" type="number" value="<%= locals.product ? product.price : '' %>">
          </div>
        </div>
      </div>
    </div>
  </div>
  
          <!-- File inputs for Images, aligned in a row -->
          <!-- <div class="form-group mt-4">
            <div class="row">
              <div class="col-md-4">
                <label class="control-label text-dark font-weight-bold errorMsg" for="image1">Image 1</label>
                <input class="form-control bg-light text-dark" name="image1" type="file" id="image1" onchange="previewImage(event, 'previewImage1')">
                <img id="previewImage1" class="previewImage" src="<%= locals.product && product.images && product.images[0] ? '/assets2/img/' + product.images[0] : '' %>" style="display: <%= locals.product && product.images && product.images[0] ? 'block' : 'none' %>; max-width: 100%;" alt="Image 1"/>
                <input type="hidden" name="existingImage1" id="existingImage1" value="<%= locals.product && product.images ? product.images[0] : '' %>">
                <% if (locals.product && product.images && product.images[0]) { %>
                  <p>Current file: <%= product.images[0] %></p>
                <% } %>
              </div>
              <div class="col-md-4">
                <label class="control-label text-dark font-weight-bold errorMsg" for="image2">Image 2</label>
                <input class="form-control bg-light text-dark" name="image2" type="file" id="image2" onchange="previewImage(event, 'previewImage2')">
                <img id="previewImage2" class="previewImage" src="<%= locals.product && product.images && product.images[1] ? '/assets2/img/' + product.images[1] : '' %>" style="display: <%= locals.product && product.images && product.images[1] ? 'block' : 'none' %>; max-width: 100%;" alt="Image 2"/>
                <input type="hidden" name="existingImage2" id="existingImage2" value="<%= locals.product && product.images ? product.images[1] : '' %>">
                <% if (locals.product && product.images && product.images[1]) { %>
                  <p>Current file: <%= product.images[1] %></p>
                <% } %>
              </div>
              <div class="col-md-4">
                <label class="control-label text-dark font-weight-bold errorMsg" for="image3">Image 3</label>
                <input class="form-control bg-light text-dark" name="image3" type="file" id="image3" onchange="previewImage(event, 'previewImage3')">
                <img id="previewImage3" class="previewImage" src="<%= locals.product && product.images && product.images[2] ? '/assets2/img/' + product.images[2] : '' %>" style="display: <%= locals.product && product.images && product.images[2] ? 'block' : 'none' %>; max-width: 100%;" alt="Image 3"/>
                <input type="hidden" name="existingImage3" id="existingImage3" value="<%= locals.product && product.images ? product.images[2] : '' %>">
                <% if (locals.product && product.images && product.images[2]) { %>
                  <p>Current file: <%= product.images[2] %></p>
                <% } %>
              </div>
            </div>
          </div> -->

          <div class="row mb-3">
            <div class="col-md-4 d-flex flex-column align-items-start">
              <label for="imageInput1" class="form-label errorMsg">Image 1</label>
              <img id="imagePreview1" class="image-preview" src="<%= locals.product && product.images && product.images[0] ? '/assets2/img/' + product.images[0] : '' %>" style="display: <%= locals.product && product.images && product.images[0] ? 'block' : 'none' %>; max-width: 100%; margin-top: 10px;" alt="Image 1" />
              <input class="form-control my-2" type="file" id="imageInput1" name="image1" onchange="openCropper(event, 'imagePreview1')">
              <input type="hidden" name="existingImage1" id="existingImage1" value="<%= locals.product && product.images ? product.images[0] : '' %>">
              <% if (locals.product && product.images && product.images[0]) { %>
                <p>Current file: <%= product.images[0] %></p>
              <% } %>
            </div>
            <div class="col-md-4 d-flex flex-column align-items-start">
              <label for="imageInput2" class="form-label errorMsg">Image 2</label>
              <img id="imagePreview2" class="image-preview" src="<%= locals.product && product.images && product.images[1] ? '/assets2/img/' + product.images[1] : '' %>" style="display: <%= locals.product && product.images && product.images[1] ? 'block' : 'none' %>; max-width: 100%; margin-top: 10px;" alt="Image 2" />
              <input class="form-control my-2" type="file" id="imageInput2" name="image2" onchange="openCropper(event, 'imagePreview2')">
              <input type="hidden" name="existingImage2" id="existingImage2" value="<%= locals.product && product.images ? product.images[1] : '' %>">
              <% if (locals.product && product.images && product.images[1]) { %>
                <p>Current file: <%= product.images[1] %></p>
              <% } %>
            </div>
            <div class="col-md-4 d-flex flex-column align-items-start">
              <label for="imageInput3" class="form-label errorMsg">Image 3</label>
              <img id="imagePreview3" class="image-preview" src="<%= locals.product && product.images && product.images[2] ? '/assets2/img/' + product.images[2] : '' %>" style="display: <%= locals.product && product.images && product.images[2] ? 'block' : 'none' %>; max-width: 100%; margin-top: 10px;" alt="Image 3" />
              <input class="form-control my-2" type="file" id="imageInput3" name="image3" onchange="openCropper(event, 'imagePreview3')">
              <input type="hidden" name="existingImage3" id="existingImage3" value="<%= locals.product && product.images ? product.images[2] : '' %>">
              <% if (locals.product && product.images && product.images[2]) { %>
                <p>Current file: <%= product.images[2] %></p>
              <% } %>
            </div>
          </div>
  
          <!-- Submit Button -->
          <div class="form-group mt-3 text-center">
            <button type="submit" id="editProductBtn" name="editProductBtn" class="btn btn-primary text-white mt-2" data-product-id="<%= productId %>">Submit</button>
            <a href="/admin/productManagement"><button type="button" id="editProductCancel" name="editProductCancel" class="btn btn-primary text-light mt-2">Cancel</button></a>
          </div>
        </fieldset>
      </form>
    </div>
  </div>






<!-- //////////////////////////////////////// -->

          </div>
        </div>

        <!-- <footer class="footer">
          <div class="container-fluid d-flex justify-content-between">
            <nav class="pull-left">
              <ul class="nav">
                <li class="nav-item">
                </li>
              </ul>
            </nav>
            </div>
          </div>
        </footer>
      </div>
    </div> -->

    <div id="cropperModal" class="modal">
      <div class="modal-content">
        <img id="cropperImage" src="" alt="Image for cropping" style="max-width: 100%;"/>
        <button onclick="cropImage()">Crop</button>
        <button onclick="closeModal()">Cancel</button>
      </div>
    </div>

    <script src="https://cdnjs.cloudflare.com/ajax/libs/cropperjs/1.5.12/cropper.min.js"></script>
    <script>
      let cropper;
      let currentImagePreviewId;
    
      function openCropper(event, previewId) {
        currentImagePreviewId = previewId;
        const file = event.target.files[0];
        if (file) {
          const reader = new FileReader();
          reader.onload = function(e) {
            const cropperImage = document.getElementById('cropperImage');
            cropperImage.src = e.target.result;
            cropperImage.onload = () => {
              if (cropper) cropper.destroy();
              cropper = new Cropper(cropperImage, {
                aspectRatio: 1,
                viewMode: 1
              });
              document.getElementById('cropperModal').style.display = 'flex';
            };
          };
          reader.readAsDataURL(file);
        }
      }
    
      function cropImage() {
        if (!cropper) return;
        const canvas = cropper.getCroppedCanvas({
          width: 800,
          height: 800
        });
        canvas.toBlob(blob => {
          const fileInput = document.querySelector(`input[id="${currentImagePreviewId.replace('Preview', 'Input')}"]`);
          const fileInputData = new DataTransfer();
          const file = new File([blob], 'cropped-image.jpg', { type: 'image/jpeg' });
          fileInputData.items.add(file);
          fileInput.files = fileInputData.files;
          document.getElementById(currentImagePreviewId).src = canvas.toDataURL('image/jpeg', 0.9); // Set quality
          closeModal();
        }, 'image/jpeg', 1);
      }
    
      function closeModal() {
        document.getElementById('cropperModal').style.display = 'none';
        if (cropper) cropper.destroy();
      }
    </script>

    <!-- Image preview -->
    <script>

      const editProductForm = document.getElementById('editProductForm');
        const productName = document.getElementById('productName');
        const model = document.getElementById('model');
        const description = document.getElementById('description');
        const type = document.getElementById('type');
        const strapType = document.getElementById('strapType');
        const color = document.getElementById('color');
        const category = document.getElementById('category');
        const brand = document.getElementById('brand');
        const stock = document.getElementById('stock');
        const price = document.getElementById('price');
        const image1 = document.getElementById('imageInput1');
        const image2 = document.getElementById('imageInput2');
        const image3 = document.getElementById('imageInput3');
        const existingImage1 = document.getElementById('existingImage1');
        const existingImage2 = document.getElementById('existingImage2');
        const existingImage3 = document.getElementById('existingImage3');

        editProductForm.addEventListener('submit', (e) => {
        e.preventDefault();

        validateProduct();
        });

        const setEditProdError = (element, message) => {
            const formGroup = element.parentElement;
            const errorDisplay = formGroup.querySelector('.errorMsg');
        

        errorDisplay.innerText = message;
        element.classList.add('error');
        errorDisplay.classList.add('error');
        element.classList.remove('success');
        errorDisplay.classList.remove('success');

        };

        const setEditProdSuccess = (element, message) => {
            const formGroup = element.parentElement;
            const errorDisplay = formGroup.querySelector('.errorMsg');

        errorDisplay.innerText = message;
        element.classList.add('success');
        errorDisplay.classList.add('success');
        element.classList.remove('error');
        errorDisplay.classList.remove('error');

        };

        const isValidName = categoryValue => {
            let regex = /^[a-zA-Z0-9\s\-_]+$/;
            return regex.test(categoryValue);
        }

        const isValidImageFile = (file) => {
          const validTypes = ['image/jpeg', 'image/png'];
          const maxSize = 5 * 1024 * 1024;
          return validTypes.includes(file.type) && file.size <= maxSize;
        };


        async function validateProduct () {

          const editProductBtn = document.getElementById('editProductBtn');
          const productId = editProductBtn.dataset.productId;
          const productNameValue = productName.value.trim();
          const modelValue = model.value.trim();
          const descriptionValue = description.value.trim();
          const typeValue = type.value.trim();
          const strapTypeValue = strapType.value.trim();
          const colorValue = color.value.trim();
          const categoryValue = category.value.trim();
          const brandValue = brand.value.trim();
          const stockValue = stock.value.trim();
          const priceValue = price.value.trim();

          const image1Value = image1.files[0];
          const image2Value = image2.files[0];
          const image3Value = image3.files[0];

          const existingImage1Value = existingImage1.value;
          const existingImage2Value = existingImage2.value;
          const existingImage3Value = existingImage3.value;

          let isValid = true;

        if(!productNameValue) {
            setEditProdError(productName, "*Please enter the product name");
            isValid = false;
        }
        else {
            setEditProdSuccess(productName, "Product Name");
        }

        if(!modelValue) {
            setEditProdError(model, "*Please enter the Model");
            isValid = false;
        }
        else {
            setEditProdSuccess(model, "Model");
        }

        if(!descriptionValue) {
            setEditProdError(description, "*Please enter the description");
            isValid = false;
        }
        else {
            setEditProdSuccess(description, "Description");
        }

        if(!typeValue) {
            setEditProdError(type, "*Please select the type");
            isValid = false;
        }
        else {
            setEditProdSuccess(type, "Type");
        }

        if(!strapTypeValue) {
            setEditProdError(strapType, "*Please enter the strap type");
            isValid = false;
        }
        else {
            setEditProdSuccess(strapType, "Strap Type");
        }

        if(!colorValue) {
            setEditProdError(color, "*Please enter the color");
            isValid = false;
        }
        else {
            setEditProdSuccess(color, "Color");
        }

        if(!categoryValue) {
            setEditProdError(category, "*Please select the category");
            isValid = false;
        }
        else {
            setEditProdSuccess(category, "Category");
        }

        if(!brandValue) {
            setEditProdError(brand, "*Please select the brand");
            isValid = false;
        }
        else {
            setEditProdSuccess(brand, "Brand");
        }

        if(!stockValue) {
            setEditProdError(stock, "*Please enter the stock");
            isValid = false;
        }
        else {
            setEditProdSuccess(stock, "Stock");
        }

        if(!priceValue) {
            setEditProdError(price, "*Please enter the price");
            isValid = false;
        }
        else {
            setEditProdSuccess(price, "Price");
        }

        if(!image1Value && !existingImage1Value) {
            setEditProdError(imageInput1, "*Please select an image");
            isValid = false;
        }
        else if (image1Value && !isValidImageFile(image1Value)) {
          setEditProdError(imageInput1, "*Please select a valid image (jpeg/png) under 5 mb");
          isValid = false;
        }
        else {
            setEditProdSuccess(imageInput1, "Image 1");
        }

        if(!image2Value && !existingImage2Value) {
            setEditProdError(imageInput2, "*Please select an image");
            isValid = false;
        }
        else if (image2Value && !isValidImageFile(image2Value)) {
          setEditProdError(imageInput2, "*Please select a valid image (jpeg/png) under 5 mb");
          isValid = false;
        }
        else {
            setEditProdSuccess(imageInput2, "Image 2");
        }


        if(!image3Value && !existingImage3Value) {
            setEditProdError(imageInput3, "*Please select an image");
            isValid = false;
        }
        else if (image3Value && !isValidImageFile(image3Value)) {
          setEditProdError(imageInput3, "*Please select a valid image (jpeg/png) under 5 mb");
          isValid = false;
        }
        else {
            setEditProdSuccess(imageInput3, "Image 3");
        }

        if(!isValid) {
          return;
        }

        const formData = new FormData();
          formData.append('productName', productNameValue);
          formData.append('model', modelValue);
          formData.append('description', descriptionValue);
          formData.append('type', typeValue);
          formData.append('strapType', strapTypeValue);
          formData.append('color', colorValue);
          formData.append('category', categoryValue);
          formData.append('brand', brandValue);
          formData.append('stock', stockValue);
          formData.append('price', priceValue);
          formData.append('image1', image1.files[0]);
          formData.append('image2', image2.files[0]);
          formData.append('image3', image3.files[0]);
          formData.append('existingImage1', existingImage1Value);
          formData.append('existingImage2', existingImage2Value);
          formData.append('existingImage3', existingImage3Value);

        const response = await fetch(`/admin/editProduct/${productId}`, {
                method: "POST",
                body: formData
            });

            const productData = await response.json();

            if (productData.isActive === false) {
              window.location.href = '/admin/login';
            }
            else if(productData.success) {
                    window.location.href = "/admin/productManagement?message=Product added successfully...";
            }
            else if(response.ok) {
                const errorDisplay = document.querySelector('.main-error');
                const productName = document.getElementById('productName');

                errorDisplay.innerText = productData.message;
                productName.classList.add('error');
                productName.classList.remove('success');
            }
        }
    </script>

    <!-- block btn toggle -->
    <script src="/assets2/js/adminScript.js"></script>
    <!--   Core JS Files   -->
    <script src="/assets2/js/core/jquery-3.7.1.min.js"></script>
    <script src="/assets2/js/core/popper.min.js"></script>
    <script src="/assets2/js/core/bootstrap.min.js"></script>
    <!-- jQuery Scrollbar -->
    <script src="/assets2/js/plugin/jquery-scrollbar/jquery.scrollbar.min.js"></script>
    <!-- Chart JS -->
    <script src="/assets2/js/plugin/chart.js/chart.min.js"></script>
    <!-- jQuery Sparkline -->
    <script src="/assets2/js/plugin/jquery.sparkline/jquery.sparkline.min.js"></script>
    <!-- Chart Circle -->
    <!-- Datatables -->
    <script src="/assets2/js/plugin/datatables/datatables.min.js"></script>
    <!-- Bootstrap Notify -->
    <script src="/assets2/js/plugin/bootstrap-notify/bootstrap-notify.min.js"></script>
    <!-- jQuery Vector Maps -->
    <script src="/assets2/js/plugin/jsvectormap/jsvectormap.min.js"></script>
    <script src="/assets2/js/plugin/jsvectormap/world.js"></script>
    <!-- Google Maps Plugin -->
    <script src="/assets2/js/plugin/gmaps/gmaps.js"></script>
    <!-- Sweet Alert -->
    <script src="/assets2/js/plugin/sweetalert/sweetalert.min.js"></script>
    <!-- Kaiadmin JS -->
    <script src="/assets2/js/kaiadmin.min.js"></script>
    <!-- Sweet Alert -->
    <script src="https://cdn.jsdelivr.net/npm/sweetalert2@11"></script>
    <!-- cropping JS -->
    <script src="https://unpkg.com/cropperjs/dist/cropper.min.js"></script>

    
  </body>
</html>